/**
 * Wiki path constant. The wiki surface itself is retired — `/wiki*` redirects
 * home (see App.tsx) — this constant exists only so that redirect check has
 * one place to point at.
 */
export const WIKI_PATH = "/wiki";
