/**
 * Wave 3a — re-export kit `DataState`.
 *
 * Wave 2 mapped the kit API onto Carbon `SkeletonText` + `Tile`. Wave 3a
 * restores the kit primitive so adapter import paths keep working.
 */
export { DataState } from "@hcw/ui-kit";
export { DataState as default } from "@hcw/ui-kit";
