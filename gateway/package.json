{
  "name": "@esti/jobs-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Authenticated HTTP boundary in front of the Redis Streams job bus — see docs/esti/NEXTJS-MIGRATION-PHASE6-AUDIT.md § the enqueue boundary.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts"
  },
  "dependencies": {
    "ioredis": "^5.4.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
