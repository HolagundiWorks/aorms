import { RouteLoading } from "../../components/aorms/RouteLoading";

/** Route-level loading skeleton for the Client Portal — see
 * RouteLoading.tsx's own comment. */
export default function PortalLoading() {
  return <RouteLoading />;
}
