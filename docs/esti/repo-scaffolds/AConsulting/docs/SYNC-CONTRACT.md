# Sync contract pin

Identical pin to AStudio — esti HUB-API `2026-08` + PORTAL-SYNC-BRIDGE
`2026-08-bridge`. Same allow-list and never-sync rules. Keep this file in sync
with AStudio `docs/SYNC-CONTRACT.md` when the hub bumps.
